# מה השתנה בסבב הזה (v7)

## קבצים ששונו

| קובץ | מה השתנה |
|---|---|
| `client/src/games/dollar/DollarGame.jsx` | עוברים שלב רק אחרי **5 ריענוני לוח** (5 גלי מטבעות של 5 שניות), גם אם לא נאספו כל המטבעות. כישלון מאפס את הספירה. נוספו 8 כוחות-על ייעודיים. |
| `client/src/games/car/CarGame.jsx` | נוספו 7 כוחות-על ייעודיים למכונית, ומגן שסופג התנגשות. |
| `client/src/games/shapes/ShapesGame.jsx` | נוספו 8 כוחות-על ייעודיים (חצי-חצי, רמז, זמן, פנס קסם ועוד) ותמיכה במגן. |
| `client/src/games/shapes/shapeData.js`, `questions.js` | **מפירות לחיות**: 34 חיות ב-7 קבוצות (חיות בית, משק, בר, ים, ציפורים, חרקים, זוחלים). החיות הן אמוג'י בתוך SVG, כך שהצללית נשארת שחורה כמו קודם. |
| `client/src/shared/prizes.js` | הקטלוג עדיין 100 פרסים: דילוג שלב (עד 2 שימושים, כמו קודם), 30 כוחות-על, והשאר עיצובים. בחירת הפרס מציעה דילוג + 2 כוחות-על (לפעמים עיצוב במקום אחד מהם). |
| `client/src/hooks/useGameEngine.js` | `levelUpMode: 'external'` + `completeLevel`, מגן אוטומטי ב-`registerFailure`, הפעלת כוחות-על (`activatePowerup`), כפל ניקוד בזמן. |
| `client/src/storage/prizesStorage.js` | תיק כוחות-על נשמר בדפדפן (כמו שאר הפרסים). |
| `client/src/screens/GameScreen.jsx` | הדולר עובר ל-`levelUpMode: 'external'`, ושורת כוחות-על מתחת לסרגל העליון. |
| `client/src/components/PrizeSelectionOverlay.jsx` | מוצג תיאור קצר לכל פרס. |
| `client/src/styles/index.css`, `client/src/shared/games.js`, `server/src/games.js`, `README.md` | עיצוב שורת הכוחות, אייקון משחק הצורות 🍎 → 🦁, ותיאור. |

## קובץ חדש

`client/src/components/PowerupBar.jsx` — שורת כוחות-על: לחיצה או מקש מספר (1-9) מפעילה. מגן פועל אוטומטית. אם כוח-על לא מתאים לרגע (למשל פנס קסם כשאין צללית), הוא נשאר בתיק.

## כוחות-על לפי משחק

- **כולם:** מגן (סופג טעות), מגן כפול, בונוס נקודות, בונוס גדול, תיבת אוצר, כפל ניקוד ×2 / ×3 בזמן.
- **דולר:** הקפאת שודדים, האטת שודדים, מגנט מטבעות, גל הדף, שטר רוח רפאים, שטר מכווץ, שטר דמה, שטר זריז.
- **מכונית:** האטת הכביש, עצירת תנועה, מצב רוח רפאים, מכונית מיני, מטאטא כביש, GPS, היגוי ספורט.
- **צורות:** חצי-חצי, רמז מהבהב, +5 / +10 שניות, הקפאת זמן, פנס קסם, שאלה חדשה, פתרון מיידי.

## החלטות שכדאי שתדעי עליהן

- מגן שסופג פגיעה במכונית/דולר נותן 2 שניות חסינות, כדי שנגיעה אחת לא תבזבז את כל המגנים.
- מגנט המטבעות במשחק הדולר לא פותח אתגר מדליה (רק ניקוי גל בכוחות עצמך).
- הפרסים נמצאים ב-`prizes.js`. כדי להוסיף כוח-על חדש, מוסיפים שורה ל-`POWERUPS` וטיפול ב-`powerHandlerRef` של המשחק.

---

# מה השתנה בסבב הזה

הפרויקט המצורף הוא **כל** הפרויקט שלך, מעודכן. הכי פשוט: תחליפי את כל
תיקיית `medal-battle` הישנה בזו שבתוך ה-zip (או תעתיקי לפחות את התיקייה
`client/` ו-`server/` מכאן על אלה שיש לך). אין צורך "להדביק" קטעי קוד -
כל קובץ כבר שלם ומוכן.

להרצה: `cd server && npm install && npm start`, ובטרמינל נפרד
`cd client && npm install && npm run build` (או `npm run dev` לפיתוח).

---

## 1. קבצים ששונו

| קובץ | מה השתנה |
|---|---|
| `client/src/shared/scoring.js` **וגם** `server/src/scoring.js` (זהים, כמו תמיד) | `levelConfig()`: מכפיל המהירות (`speedFactor`) גדל לאט יותר ויש לו תקרה (2.1×) - זו הייתה סיבת התחושה של "בורח מהר מדי". נוספו `lanes` ו-`blockedLanes` למשחק המכונית, ו-`hazards` למשחק הדולר עולה בהדרגה במקום קפיצה מיידית. |
| `client/src/games/car/CarGame.jsx` | הנתיבים כבר לא קבועים (5) - עולים עם השלב עד תקרה של 6. כמות המכשולים החוסמים שורה עולה עד תקרה של 3 (במקום להיגזר מ-`hazards`). הרווח של 2 "רכבים" בין מכשולים כבר היה קיים - נשמר. מהירות הבסיס הואטה קלות (150 במקום 190) לתחושת שליטה יציבה יותר. תומך גם בפרס עיצובי (צבע/דגם) אם נרכש. |
| `client/src/games/dollar/DollarGame.jsx` | קורא את `hazards` המעודכן (מתחיל נמוך, עולה בהדרגה, תקרה גבוהה יותר בשלבים מתקדמים). תומך בפרס עיצובי (עיצוב שטר) אם נרכש. |
| `client/src/components/TopBar.jsx` | שונה לגמרי לפי הבקשה: רק **שני** תאים בעיצוב כרטיס - "זמן משחק" ו"ניקוד מחושב" (הדירוג). שלב, נקודות-לשאלה וניקוד גולמי מוצגים כשורת טקסט פשוטה בלי מסגרת. |
| `client/src/components/Leaderboard.jsx` | כל שורה היא עכשיו מלבן קטן ונפרד עם תג מקום עגול ובולט; הניקוד מוצג קטן ועדין בפינה (לא בולט כמו המקום). |
| `client/src/screens/GameScreen.jsx` | נוספו: `PrizeCart` (סל הקניות) בתפריט הצדדי, `MedalChallengeOverlay` ו-`PrizeSelectionOverlay`. |
| `client/src/screens/LobbyScreen.jsx` | נוסף סרגל ניווט עליון (בית / נתונים / אודות / צור קשר) שמחליף בין הקומפוננטות החדשות. |
| `client/src/hooks/useGameEngine.js` | הלב של פיצ'ר המדליה: זיהוי "ענית תוך 3 שניות" או "3 שלבים בלי טעות", פתיחת אתגר מדליה, מענה עליו, פתיחת בחירת פרס כל 3 מדליות, ומימוש פרס הדילוג. |
| `client/src/styles/index.css` | עיצוב לכל מה שלמעלה (TopBar, Leaderboard, אתגר מדליה, בחירת פרס, סל קניות, ניווט הלובי, נתוני המשחקים). |

## 2. קבצים חדשים

| קובץ | מה זה |
|---|---|
| `client/src/shared/riddles.js` | מאגר 100 החידות (20 חידות היגיון שנכתבו ידנית + 80 חידות חשבון/סדרות שנבנות אוטומטית בקוד - ר' ההערה בראש הקובץ). |
| `client/src/shared/prizes.js` | קטלוג 100 הפרסים: פרס הדילוג (מוגבל ל-2), ו-99 פרסי עיצוב (צבעים/דגמים) למכונית ולדולר. |
| `client/src/storage/prizesStorage.js` | שמירה בדפדפן של הפרסים שנרכשו ומונה השימושים בפרס הדילוג - **גלובלי לכל המשתמשת**, לא לפי משחק. |
| `client/src/components/MedalChallengeOverlay.jsx` | מסך אתגר המדליה (החידה + טיימר 20 שניות). |
| `client/src/components/PrizeSelectionOverlay.jsx` | מסך בחירת הפרס, כולל אזהרת האישור לפרס הדילוג. |
| `client/src/components/PrizeCart.jsx` | תצוגת "סל הקניות" בתפריט הצדדי. |
| `client/src/components/GamesInfo.jsx` | קומפוננטת "נתונים על המשחקים" לדף הבית. |
| `client/src/components/AboutPage.jsx` | קומפוננטת "אודות". |
| `client/src/components/ContactPage.jsx` | קומפוננטת "צור קשר" (שולחת מייל דרך `mailto:` - אין צורך בשרת ייעודי; אפשר להחליף את הכתובת בראש הקובץ). |

## 3. הערות/החלטות שכדאי שתדעי עליהן

- **"ענית תוך 3 שניות"** מדויק לגמרי במשחק הצורות (יש שם טיימר שאלה אמיתי). במכונית ובדולר אין "שאלה" עם דדליין מפורש, אז זה מחושב לפי אותו יחס-מהירות שהמשחקים כבר מדווחים על כל הצלחה - קירוב סביר, לא מדידת זמן מדויקת של 3 שניות ממש.
- מאגר החידות "מתאפס" (מתחיל סבב חדש) אם כל 100 כבר נשאלו באותה טעינת דף - זה כמעט לא יקרה במשחק רגיל.
- פרס הדילוג מוגבל ל-2 שימושים **לכל המשתמשת/לדפדפן**, לא לכל משחק בנפרד - כפי שביקשת ("לכל משתמש").
---

# עדכון נוסף: תיקון מנגנון המטבעות במשחק הדולר

הבעיה שדיווחת עליה: "ענית תוך 3 שניות" כמעט תמיד היה נכון בתפיסת מטבע
בודד, אז אתגר המדליה נפתח כל הזמן בלי קשר למיומנות אמיתית. זה תוקן.

## מה השתנה עכשיו

| קובץ | מה השתנה |
|---|---|
| `client/src/hooks/useGameEngine.js` | `registerSuccess` מקבל עכשיו פרמטר שני אופציונלי `{ skipFastCheck }` שמדלג על בדיקת "3 שניות" עבור קריאות ספציפיות. נוסף גם `triggerMedalChallenge` לרשימת הפעולות שהמנוע מחזיר, כדי שמשחק בודד (כמו הדולר) יוכל להפעיל אתגר מדליה בתנאי הצלחה שהוא מגדיר בעצמו. |
| `client/src/games/dollar/DollarGame.jsx` | נכתב מחדש: במקום מטבע בודד עם "שעון חיים" - **גל שלם של 5 עד 10 מטבעות** (עולה עם השלב) מופיע במסך בו-זמנית, יחד עם השודדים. **כל 4 שניות בדיוק נולד גל חדש**, בלי קשר אם תפסת את כל הקודם. אם מצליחים לתפוס **את כל** מטבעות הגל לפני שהוא מתחלף - זה מה שפותח ניסיון לתפוס חלק במדליה (בדיוק כמו "3 שלבים נקיים" במשחקים האחרים, רק שהתנאי כאן הוא "ניקוי גל שלם"). כל תפיסת מטבע בודדת עדיין נותנת ניקוד רגיל, רק לא מפעילה יותר את בדיקת "3 השניות" הכללית. נוסף גם פס התקדמות עדין למעלה שמראה כמה זמן נשאר לגל הנוכחי.

הזרימה הכללית (כל 3 מדליות → בחירת פרס, פרס הדילוג מוגבל ל-2 וכו') נשארה בדיוק אותו דבר - רק "איך נפתח אתגר מדליה" במשחק הדולר ספציפית השתנה.


---

# עדכון נוסף: תוקן באג - הגל התאפס בכל תפיסת מטבע

**הבאג שדיווחת עליו**: המסך היה "מתרנדר" (מאפס את גל המטבעות) בכל תפיסת
מטבע בודדת, במקום רק כל כמה שניות.

**הסיבה בפועל**: `DollarGame.jsx` איפס את הגל דרך `roundKey` של המנוע -
אבל `roundKey` מתעדכן ב-`useGameEngine.js` אחרי **כל** תשובה נכונה (זה
נכון ורצוי במשחקי הצורות/המכונית, ששם כל תשובה נכונה = שאלה/מכשול הבא).
במשחק הדולר זה גרם לכך שכל תפיסת מטבע מחקה את כל הגל ויצרה אחד חדש.

**התיקון**: `client/src/games/dollar/DollarGame.jsx` — איפוס הזירה כבר
לא תלוי ב-`roundKey`. הוא תלוי עכשיו רק באירועים אמיתיים של התחלת שלב:
טעינת המשחק, עליית שלב (`level` משתנה), וחזרה ממסך כישלון. תפיסת מטבע
בודדת כבר לא נוגעת בגל בכלל - רק הטיימר הפנימי (עכשיו **5 שניות**, כפי
שביקשת, במקום 4) מחליף גל.

---

# עדכון נוסף: עליית שלב לפי ניקוד במשחק הדולר + תיקון תצוגת הניקוד

## 1. הדולר עולה שלב רק אחרי 20 נקודות (לא כל רגע)

**הבעיה**: עליית שלב הייתה מבוססת על *מספר* הצלחות (5 תפיסות מטבע =
שלב), בלי קשר לכמה נקודות כל תפיסה שווה בפועל. זה גרם לתחושת "עובר שלב
כל רגע".

**התיקון**:
- `client/src/hooks/useGameEngine.js` — נוספו שני פרמטרים אופציונליים: `levelUpMode` ('count' כברירת מחדל, או 'points') ו-`levelUpThreshold`. ב-`levelUpMode: 'points'` המנוע סופר **ניקוד מצטבר בתוך השלב** במקום מספר הצלחות, ועולה שלב רק כשהניקוד הזה מגיע לסף שנקבע.
- `client/src/screens/GameScreen.jsx` — משחק הדולר מוגדר עכשיו עם `{ levelUpMode: 'points', levelUpThreshold: 20 }`. משחקי הצורות והמכונית ממשיכים לעבוד בדיוק כמו קודם (5 הצלחות = שלב) - שום דבר לא השתנה אצלם.

## 2. תוקנה בעיית "הניקוד זז אחורה"

**הבעיה בפועל**: תא "הניקוד המחושב" ב-TopBar הציג את `rating()` - נוסחה
שמחשבת "נקודות לדקה" מחדש כל רגע. הבעיה: אם עובר זמן בלי לצבור נקודות
חדשות, המכנה (הדקות) גדל בזמן שהמונה (הנקודות) נשאר קבוע - והתוצאה
המוצגת *יורדת*, גם בלי לעשות שום דבר לא נכון. זה בדיוק מה שנראה כ"זז
אחורה" ובלבל.

**התיקון**: `client/src/components/TopBar.jsx` — התא "🧮 ניקוד מחושב"
מציג עכשיו את `points` עצמו: הניקוד הסופי, המצטבר, שרק עולה ולעולם לא
יורד. בונוס המהירות עדיין מחושב "מאחורי הקלעים" בכל הצלחה בודדת (ר'
`answerPoints()` ב-`shared/scoring.js` - שם בסיס + בונוס מהירות מחושבים
יחד באותו רגע ומתווספים ישירות ל-`points`), כך שהתוצאה המוצגת כבר כוללת
את הבונוס - רק בלי הנוסחה שמסתכלת אחורה על "כמה זמן עבר" וגורמת לירידה
מבלבלת. `rating()` (הדירוג המשולב נקודות+זמן) עדיין קיימת ועדיין
משמשת ל**מיון לוח התוצאות בשרת** - שם היא מחושבת פעם אחת בכל סנכרון
ולא "בזמן אמת מול העיניים", כך שאין שם את אותה בעיה.

## הערה: עבודת המכונית מהבקשה הקודמת עדיין לא הושלמה

בהודעה הקודמת ביקשת גם לתקן את משחק המכונית (מערך פרסים נפרד, מדליה
כל 3 שלבים נקיים, פרס כל 2 מדליות, מדרגות נתיבים/מכשולים מפורשות, תיקון
יציבות, ותיקון "המכונית חוזרת למרכז"). התחלתי רק את חלק הנוסחאות
(`shared/scoring.js` כבר כולל `carLanesForLevel`, `carBlockedLanesForLevel`
ו-`carSpeedFactor`), אבל **`CarGame.jsx` עצמו עדיין לא עודכן** להשתמש
בהם, ומערך הפרסים הנפרד עדיין לא נוצר. תגידי לי אם להמשיך עם זה עכשיו.

---

# עדכון נוסף: מדרגות קושי חדשות למכונית + הקטנת כביש/רכב לראייה מרחוק + תיקון באג חוזר

## 1. מדרגות נתיבים/מכשולים חדשות (לפי בקשה מפורשת)

`client/src/shared/scoring.js` (וגם `server/src/scoring.js`, זהים כרגיל):

| שלב | נתיבים | מכוניות-מכשול חוסמות |
|---|---|---|
| 1-19 | 2 | 1 |
| 20-29 | 5 | 2 |
| 30-49 | 8 | 3 |
| 50+ | 9 | 4 |

## 2. כביש ורכב קטנים יותר = רואים מרחוק ויש יותר זמן תגובה

`client/src/games/car/CarGame.jsx` — המכונית והמכשולים הוקטנו (רוחב/גובה
המכונית, גובה המכשולים), ומרווח ה"התראה מראש" (`TOP_SPAWN_MARGIN`) הוגדל.
התוצאה: יותר שורות מכשולים נראות בבת אחת על אותו גובה מסך - בלי לשנות
את מהירות הנסיעה בפועל - וזה בדיוק מה שנותן יותר זמן לתכנן ולעקוף.

## 3. תוקן שוב באג "המכשולים בורחים/נעלמים"

זיהיתי שהבאג חזר: קוד האיפוס של הכביש היה תלוי ב-`roundKey` של המנוע,
ו-`roundKey` מתקדם אחרי **כל** הצלחה בודדת (גם במצב `levelUpMode:'time'`
שהמכונית משתמשת בו כרגע) - כך שכל שורת מכשולים שנעקפה בהצלחה מחקה
ויצרה מחדש את כל הכביש, ואת מיקום המכונית (שחזרה למרכז). התיקון: איפוס
הכביש תלוי עכשיו רק בשינוי אמיתי במבנה הנתיבים (עליית שלב עם יותר
נתיבים) או בחזרה ממסך כישלון - ומיקום המכונית מתאפס פעם אחת בלבד,
בטעינת המשחק.
